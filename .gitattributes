# Normalise line endings so the repository is identical on Windows, Linux and the
# GitHub Actions runners.
* text=auto eol=lf

*.bat   text eol=crlf
gradlew text eol=lf

*.jar  binary
*.webp binary
*.png  binary
